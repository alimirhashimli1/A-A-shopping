import React from "react";
import Carousel from "../views/Carousel";
const Logout = props => {
    return (
      <div>
      {props.logout}
       <Carousel />
      </div>
    );
}

export default Logout;